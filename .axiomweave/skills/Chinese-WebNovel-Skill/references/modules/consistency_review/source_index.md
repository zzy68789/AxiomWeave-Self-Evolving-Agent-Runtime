# 审查模块来源索引

## 一、主框架来源

本模块不是独立理论模块，而是统筹模块。

它当前综合了：

- `transition` 模块
- `opening` 模块
- `dialogue` 模块
- `chapter_ending` 模块
- `plot_logic` 模块
- `character_consistency` 模块
- 原根层质量清单中的章节检查项

## 二、这轮重组原则

- 把“写完再看看”变成强制的章节交付流程。
- 审查维度固定为六类一致性。
- 一旦某项不过关，明确回跳到哪个模块，不再笼统说“再润一润”。
